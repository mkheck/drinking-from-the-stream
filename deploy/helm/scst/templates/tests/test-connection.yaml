apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scst.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "scst.name" . }}
    helm.sh/chart: {{ include "scst.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "scst.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
